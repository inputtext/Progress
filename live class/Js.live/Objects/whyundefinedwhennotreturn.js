var obj = {
    greet : ()=>{
        console.log("Hi how are you ");
    },
};
console.log(obj.greet());
//prints what in function and also undefined because the function doesn't returns anything
/* console.log(what obj.greet returns prints here (obj.greet(what greet returns , stored and printed here) )); */
