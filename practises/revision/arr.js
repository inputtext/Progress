let arr = [1,2,3,4,5,6];
/* arr.push(10); //pushes the element at the last
arr.pop();//pops the element from the last of the arr

arr.unshift(100); //adds the element at the first place
arr.shift(); //removes the element from the first place
 */
arr.splice(1,2); //at the 1st index , removed 2 element
console.log(arr);
